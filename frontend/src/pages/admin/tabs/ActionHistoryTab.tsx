import React from 'react';
import { Box, Typography } from '@mui/material';

const ActionHistoryTab: React.FC = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Action History Test Screen</Typography>
      <Typography>If you see this, the connection is working!</Typography>
    </Box>
  );
};

export default ActionHistoryTab;
